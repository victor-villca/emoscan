```mermaid
sequenceDiagram
    %% Título sin formato HTML
    title Finalización y generación de reporte

    %% Participantes sin formato HTML, \n para saltos de línea
    actor Usuario as Usuario
    participant Web as Aplicación\nWeb
    participant API as Backend\nAPI
    participant Generador as Generador\nde Reportes
    participant DB as Base de\nDatos
    participant Storage as Almacenamiento

    %% Inicio del flujo
    activate Usuario
    Usuario->>Web: Clic en "Finalizar sesión"
    activate Web

    Web->>API: Solicita finalización de sesión
    activate API

    API->>API: Detiene bucle de detección

    API->>Generador: Solicita generación de reporte
    activate Generador

    %% Proceso de generación del reporte (rect se mantiene)
    rect rgb(255, 245, 230)
        note right of Generador: Proceso de generación
        Generador->>DB: Recupera datos emocionales
        activate DB
        DB-->>Generador: Devuelve datos
        deactivate DB

        Generador->>Generador: Genera archivo PDF

        Generador->>Storage: Guarda archivo PDF
        activate Storage
        Storage-->>Generador: Devuelve URL del PDF
        deactivate Storage
    end

    Generador-->>API: Devuelve enlace y metadatos
    deactivate Generador

    API->>DB: Actualiza con metadatos del reporte
    activate DB
    DB-->>API: Confirmación
    deactivate DB

    API-->>Web: Envía enlace al PDF
    deactivate API

    Web-->>Usuario: Muestra o permite descarga del PDF
    deactivate Web
    deactivate Usuario

    %% Notas adicionales explicativas (\n para saltos de línea)
    note over Generador: Procesamiento de datos\nemocionales y estadísticas
    note over Storage: Almacenamiento seguro\ncon acceso controlado
    note over Web,Usuario: Experiencia de usuario\noptimizada para visualizaciónsequenceDiagram
    title Finalización de sesión y generación de reporte emocional (web + PDF opcional)
    actor Usuario as Usuario
    participant Web as Aplicación Web
    participant API as Backend API
    participant Generador as Generador de Reporte
    participant DB as Base de Datos
    participant Storage as Almacenamiento (PDF)
    %% Inicio del proceso de finalización
    activate Usuario
    Usuario->>Web: Clic en "Finalizar sesión de emociones"
    activate Web
    Web->>API: Solicita finalización de sesión
    activate API
    API->>API: Detiene el bucle de detección en backend
    %% Solicita generación del reporte dinámico (web)
    API->>Generador: Inicia proceso de análisis emocional por rostro
    activate Generador
    rect rgb(255, 245, 230)
        note right of Generador: Generación de reporte dinámico (HTML)
        Generador->>DB: Recupera timeline de emociones por rostro
        activate DB
        DB-->>Generador: Devuelve datos crudos
        deactivate DB
        Generador->>Generador: Calcula métricas por rostro:<br/>- % de duración por emoción<br/>- Frecuencia por emoción<br/>- Detección de cambios drásticos<br/>- Sparkline de emociones vs tiempo
    end
    Generador-->>API: Devuelve objeto estructurado del reporte
    deactivate Generador
    API->>DB: Guarda reporte vinculado al usuario psicólogo
    activate DB
    DB-->>API: Confirmación de guardado
    deactivate DB
    API-->>Web: Respuesta con datos para visualizar reporte en página
    deactivate API
    Web-->>Usuario: Redirige a vista de reporte dinámico (nueva página)
    deactivate Web
    %% Exportación opcional
    Usuario->>Web: Clic en "Exportar a PDF" (opcional)
    activate Web
    Web->>API: Solicita versión PDF del mismo reporte
    activate API
    API->>Generador: Convierte reporte HTML a PDF
    activate Generador
    Generador->>Storage: Guarda archivo PDF
    activate Storage
    Storage-->>Generador: Devuelve URL del PDF
    deactivate Storage
    Generador-->>API: Devuelve URL del PDF
    deactivate Generador
    API-->>Web: Retorna enlace de descarga
    deactivate API
    Web-->>Usuario: Muestra opción para descargar el PDF
    deactivate Web
    deactivate Usuario